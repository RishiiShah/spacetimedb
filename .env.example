# Client env vars (copy to .env.local — never commit .env.local)
VITE_SPACETIMEDB_HOST=
VITE_SPACETIMEDB_DB_NAME=

# Spacetime CLI (copy spacetime.local.json.example → spacetime.local.json)
# Set "database" to the same name as VITE_SPACETIMEDB_DB_NAME for publish/sql/logs.
