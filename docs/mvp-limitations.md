# MVP Limitations

- Car physics are client-predicted and not cheat-proof.
- SpaceTimeDB stores latest transforms and race events; it does not run a physics simulation.
- The first route is fixed and authored in code/JSON.
- Background city assets are visual only unless simple collision proxies are added.
- Runtime assets live under `public/assets/`.
